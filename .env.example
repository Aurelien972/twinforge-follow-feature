# TwinForgeFit - Environment Variables Template
# Copy this file to .env and fill in your actual values

# Supabase Configuration (REQUIRED)
# Get these values from your Supabase project dashboard at https://app.supabase.com
VITE_SUPABASE_URL=https://kwipydbtjagypocpvbwn.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Stripe Configuration (REQUIRED for payments)
# Get these keys from https://dashboard.stripe.com/apikeys

# Client-side publishable key (visible in frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key-here

# Server-side secret keys (for scripts and edge functions)
# NEVER expose these keys in the frontend
STRIPE_TEST_SECRET_KEY=sk_test_your-test-secret-key-here
STRIPE_LIVE_SECRET_KEY=sk_live_your-live-secret-key-here

# Google Health Connect / Fit Configuration (REQUIRED for wearable devices)
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# Follow the setup guide in /docs/wearables/HEALTH_CONNECT_SETUP.md

# Client-side OAuth Client ID (visible in frontend)
VITE_GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Redirect URI for OAuth callback (must match Google Cloud Console configuration)
VITE_GOOGLE_OAUTH_REDIRECT_URI=https://kwipydbtjagypocpvbwn.supabase.co/functions/v1/wearable-oauth-callback

# Server-side OAuth Client Secret (for Supabase Edge Functions only)
# NEVER expose this in the frontend - only use in Edge Functions
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret-here

# Important Notes:
# - All client-side variables must start with VITE_ to be accessible in the app
# - Never commit the actual .env file with real keys to git
# - Secret keys (without VITE_) are only for Node.js scripts and Supabase Edge Functions
# - For Netlify deployment, configure these variables in Site Settings > Environment Variables
# - Use test keys for development, live keys for production only
# - Google OAuth secrets must ONLY be configured in Supabase Edge Functions secrets
